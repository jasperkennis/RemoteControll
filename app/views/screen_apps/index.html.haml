<h2>Existing applications</h2>
<% if @screen_apps %>
	<ul>
	<% @screen_apps.each do |screen_app| %>
  	<li><b><%= screen_app.name %></b>
  		<% if current_user == (screen_app.user) %> |
	  		<%= link_to 'Edit', edit_screen_app_path(screen_app) %>
	     <% end %>
    </li>
	<% end %>
	</ul>
<% end %>

<% if user_signed_in? %>
	<%= link_to 'Add a new app!', new_screen_app_path() %>
<% else %>
<% end %>
