%h2 Existing applications
- if @screen_apps
  %ul
    - @screen_apps.each do |screen_app|
      %li
        %b= screen_app.name
        - if current_user == (screen_app.user)
          |
          - link_to 'Edit', edit_screen_app_path(screen_app)

- if user_signed_in?
  %p
    %a= link_to 'Add a new app!', new_screen_app_path()